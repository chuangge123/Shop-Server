spring:
  cloud:
    config:
      uri: http://localhost:9999
      profile: shop-search,eureka-client,mq,log,thymeleaf
      name: application
  rabbitmq:
    listener:
      simple:
        acknowledge-mode: manual # 设置手动ack
